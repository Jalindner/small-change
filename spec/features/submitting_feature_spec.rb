require 'rails_helper'

feature 'submitting a photo' do
  context "the user uploads an image file" do
    scenario "The user uploads an acceptable file and is taken to that submission's page" do


    end
  end

  context "the user tries to upload an unaccepted file" do
    scenario "The file is not uploaded and the user is redirected to the new submission page" do

    end
  end
end
