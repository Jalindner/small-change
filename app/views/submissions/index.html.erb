All submissions:

<% if @submissions %>
  <ol>
    <% @submissions.each do |sub| %>
      <li>Submission #<%= sub.id %>, created by <%= sub.recycler.first_name %> <%= sub.recycler.last_name %> on <%= sub.created_at %></li>
    <% end %>
  </ol>
<% end %>


